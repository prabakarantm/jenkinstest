pipeline {
   agent any
   stages {
        stage('build') {
            steps {
                //sh "./script.sh"
               def workspace = pwd()
               echo "worksp " + ${workspace}
            }
        }
    }
}
